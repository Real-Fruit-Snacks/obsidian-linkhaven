# Linkhaven project site

GitHub Pages serves `docs/` on the default branch. `index.html` is the chosen design
(Option A — "Field notes"). Before publishing, replace the `Real-Fruit-Snacks/obsidian-linkhaven`
placeholders with the real repository path (one find-replace across this file).
